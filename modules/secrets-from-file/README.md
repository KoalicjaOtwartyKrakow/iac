# Secrets from file module

Given a path to a `*.enc.json` or `*.enc.yml` file encrypted with sops creates secrets in Google
Secrets Manager with names from json/yaml keys and values from json/yaml values.
